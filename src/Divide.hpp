#ifndef DIVIDE
#define DIVIDE
#include<iostream>

class Divide{
    public:
        void disp(int a[], int n);
        int Partition(int a[], int s, int t);
        void QuickSort(int a[], int s, int t);
};

#endif  // !DIVIDE